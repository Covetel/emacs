# INSTALL #

```bash
$ cd ~/
$ git clone git@github.com:Covetel/emacs.git
$ ln -s emacs/dot_emacs.el ~/.emacs 
$ ln -s emacs/dot_emacs-custom.el ~/.emacs-custom.el 
$ ln -s emacs/dot_emacs.d/ ~/.emacs.d
```
